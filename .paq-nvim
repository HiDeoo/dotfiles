#!/usr/bin/env bash

set -e

INSTALL_FILE="${XDG_DATA_HOME:-$HOME/.local/share}"/nvim/site/pack/paqs/start/paq-nvim

if [ -d $INSTALL_FILE ]; then
  exit 0
fi

git clone --depth=1 https://github.com/savq/paq-nvim.git $INSTALL_FILE

 exit 0

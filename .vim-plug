#!/usr/bin/env bash

set -e

INSTALL_FILE="${XDG_DATA_HOME:-$HOME/.local/share}"/nvim/site/autoload/plug.vim

if [ -f $INSTALL_FILE ]; then
  exit 0
fi

curl -fLo $INSTALL_FILE --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

exit 0

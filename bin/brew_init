#!/usr/bin/env bash

if [ "`arch`" = "arm64" ]; then
  IS_M1=true
else
  IS_M1=false
fi

if type /usr/local/bin/brew > /dev/null 2>&1; then
  eval $(/usr/local/bin/brew shellenv)
fi

if $IS_M1 && type /opt/homebrew/bin/brew > /dev/null 2>&1; then
  eval $(/opt/homebrew/bin/brew shellenv)
fi
